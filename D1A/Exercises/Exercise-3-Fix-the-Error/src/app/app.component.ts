import { Component } from '@angular/core';
// NOTE: BannerComponent import is deliberately missing here.
// Students must add it to trigger and then fix the "not a known element" error.

@Component({
  selector: 'app-root',
  standalone: true,
  imports: [],
  templateUrl: './app.component.html',
  styleUrl: './app.component.css'
})
export class AppComponent { }
