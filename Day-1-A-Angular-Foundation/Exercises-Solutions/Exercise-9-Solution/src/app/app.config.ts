// SOLUTION: This file is NEW — it replaces the legacy AppModule.
// In modern Angular, app-level configuration lives in app.config.ts
// instead of being buried in an @NgModule decorator.
import { ApplicationConfig } from '@angular/core';

export const appConfig: ApplicationConfig = {
  providers: []
};
