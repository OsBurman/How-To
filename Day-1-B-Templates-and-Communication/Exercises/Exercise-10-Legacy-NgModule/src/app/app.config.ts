import { ApplicationConfig } from '@angular/core';

// MODERN app config — provides app-level configuration.
// After converting to NgModule, you'll DELETE this file.
export const appConfig: ApplicationConfig = {
  providers: []
};
