/**
 * app.config.ts — Application Configuration (Modern Pattern)
 *
 * The providers array holds application-wide services and configuration.
 * Empty for now — on Day 3 you'll add provideHttpClient(),
 * and on Day 4 you'll add provideRouter(routes).
 */

import { ApplicationConfig } from '@angular/core';

export const appConfig: ApplicationConfig = {
  providers: [
    // Empty for now — providers go here on Days 3–4
  ]
};
