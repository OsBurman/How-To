// modern/app.config.ts — Exercise 9: MODERN skeleton
//
// TODO Step 6:
// Export an appConfig that uses the modern providers array.
// This replaces AppModule as the place where app-wide configuration lives.
//
// When done, update main.ts to:
//   import { bootstrapApplication } from '@angular/platform-browser';
//   import { appConfig } from './app/modern/app.config';
//   import { AppComponent } from './app/modern/app.component';
//   bootstrapApplication(AppComponent, appConfig).catch(err => console.error(err));

import { ApplicationConfig } from '@angular/core';

// TODO: export a valid appConfig
// export const appConfig: ApplicationConfig = {
//   providers: []
// };
