/**
 * app.config.ts — Day 2 Part A: Application Configuration
 *
 * Provides application-wide configuration. At this point in the course
 * (Day 2 Part A), no providers are needed yet — HTTP and routing come later.
 */
import { ApplicationConfig } from '@angular/core';

export const appConfig: ApplicationConfig = {
  providers: [
    // No providers needed for Day 2 Part A
    // provideHttpClient() will be added on Day 3
    // provideRouter(routes) will be added on Day 4
  ]
};
