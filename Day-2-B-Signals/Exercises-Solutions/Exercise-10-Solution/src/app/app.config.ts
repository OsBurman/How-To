import { ApplicationConfig } from '@angular/core';

// Real ApplicationConfig — replaces the stub with TODO comment.
// provideAnimationsAsync() not needed for this app.
export const appConfig: ApplicationConfig = {
  providers: []
};
