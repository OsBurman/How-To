import { ApplicationConfig } from '@angular/core';

// In the standalone world, app.config.ts is the only bootstrap configuration needed.
// No NgModule, no BrowserModule, no CommonModule listed here.
export const appConfig: ApplicationConfig = {
  providers: []
};
